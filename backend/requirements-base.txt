fastapi>=0.100
uvicorn[standard]>=0.23
pillow>=10.0
piexif>=1.1.3
# numpy 2.x 허용 — <2로 고정하면 최신 Python(3.13, proot Ubuntu 등)에서
# 사전 빌드 휠이 없어 소스 컴파일에 들어가 저사양 기기 설치가 실패한다.
numpy>=1.24
# 4.10부터 numpy 2 호환
opencv-python-headless>=4.10
anthropic>=0.34
python-multipart>=0.0.6
pillow-heif>=0.16
# 임베딩 backfill의 메모리 인식 스로틀용 (없으면 /proc/meminfo 폴백)
psutil>=5.9
