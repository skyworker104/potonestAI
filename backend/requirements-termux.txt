# Termux/proot 전용 pip 목록 (install-termux.sh가 사용) — 전부 순수 파이썬(휠 무관).
#
# numpy·opencv·pillow·psutil은 apt 시스템 바이너리를 쓰고(venv --system-site-packages),
# 여기엔 어떤 Python 버전에서도 설치되는 순수 파이썬 패키지만 둔다.
# 주의: anthropic SDK는 넣지 않는다 — Rust 의존성(jiter)이 최신 Python에서
# 휠 없이 소스 빌드에 들어가 실패한다. 태블릿은 OpenRouter를 urllib로
# 직접 호출하므로 SDK가 불필요하다.
fastapi>=0.100
uvicorn[standard]>=0.23
piexif>=1.1.3
python-multipart>=0.0.6
pytesseract>=0.3
