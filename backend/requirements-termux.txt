# Termux/proot Ubuntu 전용 pip 목록 (install-termux.sh가 사용).
#
# numpy·opencv·pillow·psutil은 pip로 받으면 소스 컴파일(scikit-build-core/CMake)에
# 들어가 저사양 ARM에서 실패한다 → apt의 미리 빌드된 시스템 패키지를 쓰고
# (venv를 --system-site-packages로 생성), 여기엔 순수 파이썬/안정적 aarch64 휠만 둔다.
fastapi>=0.100
uvicorn[standard]>=0.23
piexif>=1.1.3
python-multipart>=0.0.6
anthropic>=0.34
# HEIC(아이폰 사진) — 휠 실패 시 install 스크립트가 건너뛰고 계속 진행(선택 기능)
pillow-heif>=0.16
